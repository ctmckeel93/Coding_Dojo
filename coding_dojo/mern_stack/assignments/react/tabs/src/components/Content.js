import React, {useState} from 'react'

const Context = (props) => {
    const {content, setContent} = useState('')

    const handleContext = () => {
        const selctedTab = props.selectedIndex
    }

    return 
}